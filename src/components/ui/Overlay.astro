---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className = '' } = Astro.props;
---

<div
  id={id}
  class:list={['overlay-backdrop', className]}
  role="presentation"
  hidden
>
  <slot />
</div>
